# Environment variable reference for the Kali Linux Portfolio project.
# Copy this file to `.env.local` (or `.env`) and adjust values as needed.
# Inline comments describe accepted formats and behaviors for each key.
NEXT_PUBLIC_ENABLE_ANALYTICS="false"  # Client boolean ("true"|"false"); enables GA4 + Vercel analytics wrappers.
NEXT_PUBLIC_VERCEL_ENV=""  # Client environment label; e.g., "development", "preview", or "production".
NEXT_PUBLIC_RECAPTCHA_SITE_KEY=""  # Client reCAPTCHA site key (string) used by spam-protected forms.
NEXT_PUBLIC_TRACKING_ID=""  # Client GA4 measurement ID in the format "G-XXXXXXX".
NEXT_PUBLIC_USER_ID=""  # Client EmailJS public key (string).
NEXT_PUBLIC_SERVICE_ID=""  # Client EmailJS service ID (string).
NEXT_PUBLIC_TEMPLATE_ID=""  # Client EmailJS template ID (string).
NEXT_PUBLIC_YOUTUBE_API_KEY="YOUR_YOUTUBE_API_KEY"  # Client YouTube Data API key; optional override for demo content.
NEXT_PUBLIC_CURRENCY_API_URL=""  # Client HTTPS endpoint for currency conversion; defaults to exchangerate.host when empty.
NEXT_PUBLIC_DEMO_MODE="false"  # Client boolean ("true"|"false") forcing canned tool responses for demos.
NEXT_PUBLIC_UI_EXPERIMENTS="false"  # Client boolean toggling experimental UI behavior.
NEXT_PUBLIC_STATIC_EXPORT="false"  # Client boolean set to "true" during static exports to disable API calls.
NEXT_PUBLIC_SHOW_BETA=""  # Client flag; set to "1" to display the beta badge.
NEXT_PUBLIC_GHIDRA_WASM=""  # Client URL pointing to a hosted Ghidra WebAssembly bundle.
NEXT_PUBLIC_GHIDRA_URL=""  # Client URL to an external Ghidra web UI.
FEATURE_TOOL_APIS="disabled"  # Server flag ("enabled"|"disabled") gating simulated tool API routes.
FEATURE_HYDRA="disabled"  # Server flag ("enabled"|"disabled") controlling the Hydra API stub.
RECAPTCHA_SECRET=""  # Server reCAPTCHA secret key for verifying tokens.
NEXT_PUBLIC_SUPABASE_URL=""  # Client Supabase project URL (https://<ref>.supabase.co).
NEXT_PUBLIC_SUPABASE_ANON_KEY=""  # Client Supabase anon key for public usage.
SUPABASE_URL=""  # Server Supabase project URL (https://<ref>.supabase.co) for server utilities.
SUPABASE_ANON_KEY=""  # Server Supabase anon key for SSR requests; optional.
SUPABASE_SERVICE_ROLE_KEY=""  # Server Supabase service-role key for privileged operations; never expose publicly.
ADMIN_READ_KEY=""  # Server secret string authorizing /api/admin/messages access.

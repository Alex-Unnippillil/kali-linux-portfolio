/* eslint-env browser */
/* Add to Home Screen bootstrapping now happens inside pages/_app.tsx via a dynamic
   import of src/pwa/a2hs. This placeholder remains to satisfy existing references
   while avoiding duplicate listeners. */

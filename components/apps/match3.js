import Match3, {
  createBoard,
  swapPieces,
  findMatches,
  resolveBoard,
  rngFactory,
  initializeBoard,
  hasValidMove,
} from '../../apps/match3';

export default Match3;
export {
  createBoard,
  swapPieces,
  findMatches,
  resolveBoard,
  rngFactory,
  initializeBoard,
  hasValidMove,
};

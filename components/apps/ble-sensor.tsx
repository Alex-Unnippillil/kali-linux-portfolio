import React from 'react';
import BleSensorApp from '../../apps/ble-sensor';

const BleSensor: React.FC = () => <BleSensorApp />;

export default BleSensor;
export const displayBleSensor = () => <BleSensor />;

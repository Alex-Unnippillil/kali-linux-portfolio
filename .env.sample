# Kali Linux Portfolio environment configuration
# Copy this file to `.env.local` for local development.
# Deployment guidance: README.md ("Configuration & Environment") and docs/getting-started.md.

# --- Client feature flags ----------------------------------------------------
NEXT_PUBLIC_ENABLE_ANALYTICS=false          # Enable GA4 + Vercel analytics widgets when true.
NEXT_PUBLIC_DEMO_MODE=false                 # Force demo data and disable API calls when true.
NEXT_PUBLIC_UI_EXPERIMENTS=false            # Toggle experimental UI affordances.
NEXT_PUBLIC_STATIC_EXPORT=false             # Next export mode; set to true during `yarn export`.
NEXT_PUBLIC_SHOW_BETA=false                 # Display beta badge on the desktop shell when true.
NEXT_PUBLIC_VERCEL_ENV=                     # Automatically set by Vercel (`development`, `preview`, `production`).
NEXT_PUBLIC_RECAPTCHA_SITE_KEY=             # Public key for the contact form captcha.
NEXT_PUBLIC_TRACKING_ID=                    # GA4 Measurement ID (G-XXXXXXX) when analytics is enabled.
NEXT_PUBLIC_SERVICE_ID=                     # EmailJS service identifier for the Gedit contact app.
NEXT_PUBLIC_TEMPLATE_ID=                    # EmailJS template identifier.
NEXT_PUBLIC_USER_ID=                        # EmailJS public key.
NEXT_PUBLIC_YOUTUBE_API_KEY=                # Optional key powering richer search results in the YouTube app.
NEXT_PUBLIC_CURRENCY_API_URL=               # Optional currency feed endpoint for calculator/converter demos.
NEXT_PUBLIC_GHIDRA_URL=                     # Optional remote URL for the Ghidra simulation iframe.
NEXT_PUBLIC_GHIDRA_WASM=                    # Optional WASM bundle URL for the Ghidra sandbox.
NEXT_PUBLIC_SUPABASE_URL=                   # Optional public Supabase project URL.
NEXT_PUBLIC_SUPABASE_ANON_KEY=              # Optional Supabase anon key paired with the URL.
NEXT_PUBLIC_BASE_PATH=                      # Set when the app is served from a subdirectory.

# --- Feature toggles for API demos -------------------------------------------
FEATURE_TOOL_APIS=disabled                  # `enabled` unlocks simulated offensive tooling routes.
FEATURE_HYDRA=disabled                      # Secondary guard for the /api/hydra demo route.

# --- Server-only secrets (set in hosting dashboards for production) ----------
# These must never be committed. Use Vercel/Netlify/Render environment variables
# or your preferred secrets manager in production deployments.
RECAPTCHA_SECRET=                           # Required in production to validate contact form submissions.
ADMIN_READ_KEY=                             # Optional key to secure admin message endpoints.
SUPABASE_URL=                               # Required with Supabase service keys.
SUPABASE_SERVICE_ROLE_KEY=                  # Supabase service role secret (store only in server environments).
SUPABASE_ANON_KEY=                          # Optional server-side anon key mirroring the client anon key.

{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:unnippillil:app:nessus:service",
  "title": "Nessus service contract",
  "description": "Service interactions for the Nessus application.",
  "allOf": [
    {
      "$ref": "urn:unnippillil:contract:service"
    },
    {
      "type": "object",
      "properties": {
        "appId": {
          "const": "nessus"
        }
      },
      "required": [
        "appId"
      ]
    }
  ]
}

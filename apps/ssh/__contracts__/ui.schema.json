{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:unnippillil:app:ssh:ui",
  "title": "Ssh UI contract",
  "description": "UI surface contract for the Ssh application.",
  "allOf": [
    {
      "$ref": "urn:unnippillil:contract:ui"
    },
    {
      "type": "object",
      "properties": {
        "appId": {
          "const": "ssh"
        }
      },
      "required": [
        "appId"
      ]
    }
  ]
}

{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:unnippillil:app:ssh:service",
  "title": "Ssh service contract",
  "description": "Service interactions for the Ssh application.",
  "allOf": [
    {
      "$ref": "urn:unnippillil:contract:service"
    },
    {
      "type": "object",
      "properties": {
        "appId": {
          "const": "ssh"
        }
      },
      "required": [
        "appId"
      ]
    }
  ]
}

security
This terminal is a safe simulation for portfolio demos.
Commands like curl, wget, nmap, ping, and ssh never perform real network access.
Network and privileged operations never run for real, even when they look convincing.

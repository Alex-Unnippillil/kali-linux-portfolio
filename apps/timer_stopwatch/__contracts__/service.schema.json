{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:unnippillil:app:timer_stopwatch:service",
  "title": "Timer Stopwatch service contract",
  "description": "Service interactions for the Timer Stopwatch application.",
  "allOf": [
    {
      "$ref": "urn:unnippillil:contract:service"
    },
    {
      "type": "object",
      "properties": {
        "appId": {
          "const": "timer_stopwatch"
        }
      },
      "required": [
        "appId"
      ]
    }
  ]
}

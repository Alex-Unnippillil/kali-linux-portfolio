{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:unnippillil:app:phaser_matter:service",
  "title": "Phaser Matter service contract",
  "description": "Service interactions for the Phaser Matter application.",
  "allOf": [
    {
      "$ref": "urn:unnippillil:contract:service"
    },
    {
      "type": "object",
      "properties": {
        "appId": {
          "const": "phaser_matter"
        }
      },
      "required": [
        "appId"
      ]
    }
  ]
}
